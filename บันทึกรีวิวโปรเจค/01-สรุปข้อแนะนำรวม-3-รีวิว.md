# สรุปข้อแนะนำรวมจาก 3 รีวิว (Sonnet 5 / Manus AI / Qwen) — รอรวมชุดที่ 4

วันที่: 2026-08-10 · สถานะ: **ใช้ได้แล้ว (ชุดที่ 4 ยังไม่มา — เมื่อมาแล้วจะ merge ลงท้ายเอกสารนี้)**
หลักการกรอง: ข้อเสนอทุกข้อถูกเทียบกับหลักฐานเชิงประจักษ์ EXP-012..017 — ข้อที่ขัดผลวัด = ตัด/เลื่อน ไม่รับแบบสุ่ม

---

## หมวด A — ข้อแนะนำที่เห็นด้วย + ลงมือทำได้เลย (evidence-backed, ต้นทุนต่ำ-กลาง)

| # | ข้อแนะนำ | จากรีวิว | หลักฐานยืนยัน | ต้นทุน | ลำดับ |
|---|---|---|---|---|---|
| A1 | **เพิ่ม frontend test runner (vitest)** — จับบั๊ก pattern ใน ISSUES.md (duplicate let, null ref จาก element ที่ลบแล้ว) ตั้งแต่ CI แทนเจอตอน user report | Sonnet + Qwen | ตรวจแล้ว: `frontend/package.json` ไม่มี vitest/jest; CI มีแค่ `tsc` + `vite build` | ต่ำ | 1 |
| A2 | **Auth token บน API** — ก่อนคิด expose ออก LAN; ตอนนี้ bind 127.0.0.1 ยังพอ safe แต่โค้ดเอง flag ไว้แล้ว ("No auth in v1") | Sonnet | comment ใน `api_server.py`/`hub.py` | ต่ำ | 2 |
| A3 | **Write-up EXP-012 สู่สาธารณะ** — blog / r/LocalLLaMA / Hugging Face — ใช้ honest telemetry (104 GB บน 64 GB RAM ที่ 1.5–1.9 tok/s + หลักฐาน page-fault) เป็น bait ดึง traction + ผู้ใช้จริง | Sonnet (Track A) + Qwen (#5) | repo เพิ่ง public — 0 star ปกติ ยังไม่ถึง 24 ชม. | ต่ำ | 3 |
| A4 | **README: แยก Quick Start ผู้ใช้ vs นักพัฒนา + ปรับ wording "streams weights" ให้ตรงจริง** — ระบุชัดว่า predictor ยังไม่ active (ตอนนี้ ARCHITECTURE.md บอกแล้ว แต่ README ยัง ambiguous) | Sonnet + Qwen | README ปัจจุบัน step 4 "Streams weights from NVMe" ยังไม่ชี้ว่าเป็น OS page cache/mmap | ต่ำ | 4 |
| A5 | **เผยแพร่ benchmark harness ให้คนอื่นวัดบน hardware อื่น** (Apple Silicon / AMD Ryzen AI) — เราเพิ่ม HW ไม่ได้ แต่เปิดให้ community วัดแทนเรา | Manus (#5) | EXP-012..017 harness มีอยู่แล้วใน `scripts/` — แค่จัดระเบียบ + เขียนคู่มือ | กลาง | 5 |

---

## หมวด B — ต้องตัดสินใจเชิงกลยุทธ์ก่อนทุกอย่าง (ปมเดียวที่ Sonnet เจอ อีก 2 ฝ่ายไม่เห็น)

**Track A: "honest local-inference platform"** (ของที่ shipped แล้ว — เล่นงาน telemetry/hardware advisor)
**Track B: "speculative weight streaming research"** (ของที่ตั้งเป้าไว้ — predictor ยังไม่ active)

เกณฑ์ชี้ขาดที่แนะนำ:
- ถ้าเป้าหมายคือ **ผู้ใช้ + product** → Track A (ขายของที่พิสูจน์แล้ว)
- ถ้าเป้าหมายคือ **นวัตกรรมที่ยังไม่มีใครทำ** (ตาม PROJECT.md เดิม) → Track B แต่ต้องยอมรับความเสี่ยง research
- **ทางสายกลางที่แนะนำ:** Track A เป็นหลัก (product ที่ขายได้) + Track B เป็น research spike คู่ขนาน (ไม่ผูก roadmap) — จนกว่าจะมีหลักฐานว่า LRU เริ่มไม่พอจริง

---

## หมวด C — Research path ที่ยังเปิด (ถ้าเลือก Track B / ทำเป็น spike)

| # | ข้อแนะนำ | จากรีวิว | สถานะตามหลักฐานเรา |
|---|---|---|---|
| C1 | **Predictive prefetching (expert predictor)** — ทำนาย expert ที่จะถูกใช้ แล้ว prefetch จาก NVMe ขึ้น RAM ล่วงหน้า | ทั้ง 3 ฝ่าย (ชี้ทางเดียวกัน!) | ⚠️ ยังเปิด — *prefetch timing* เป็นคนละแกนกับ placement ที่ EXP-016 ปิด; disk-bound จริง (EXP-012) แต่**ต้อง instrumented build ก่อน** (LLAMA_LOG_MOE ไม่มีใน b9967) ถึงจะ validate ได้ |
| C2 | **vLLM/SGLang integration path** — framework ที่เปิด routing internals ให้เข้าถึงง่ายกว่า llama.cpp adapter | Sonnet (Track B) | เก็บเป็นทางเลือกเมื่อถึงขั้นนั้น |
| C3 | **ทดสอบ predictor กับ Qwen3.6-35B-A3B (โมเดลที่มีอยู่แล้ว) ก่อน K3** — อย่ากระโดดข้ามขั้น | Sonnet (Track B) | ตรงกับแผน feasibility ที่เราทำไว้แล้ว |

---

## หมวด D — ปิดตามหลักฐานเชิงประจักษ์ (ไม่ทำซ้ำ ไม่ว่าใครแนะนำ)

| ข้อเสนอ | ปิดโดย | เหตุผล |
|---|---|---|
| CPU lane / expert คำนวณบน CPU | EXP-017 | CPU bandwidth-bound (ใช้แค่ 39–51% แต่ DDR4 อิ่ม) |
| Speculative decode (MTP/DSpark) | EXP-015 | ช้าลง 11–18% บน 12 GB — bandwidth-bound |
| Census → auto tiering | EXP-016 | expert activation flat ข้าม layer — auto-fit คือ optimal อยู่แล้ว |
| LAN / distributed weight streaming | ขัดฟิสิกส์ | LAN (10GbE ≈ 1.25 GB/s) ช้ากว่า NVMe (3.5+ GB/s) |
| Near-storage compute | เลื่อนระยะยาว | ต้อง custom hardware/FPGA — เกินขอบเขตตอนนี้ |

---

## หมวด E — รอรวมชุดที่ 4 (เมื่อมาแล้ว)

- [ ] ชุดที่ 4 มา → เพิ่มหัวข้อนี้ + merge เข้ากับหมวด A–D ข้างบน (เดี๋ยวจะ merge ให้ทั้ง 4 ชุดเป็นฉบับสุดท้าย)
- ประเด็นที่ตอนนี้มี "ผู้สนับสนุนแค่คนเดียว" รอฝ่ายที่ 4 ยืนยัน: Docker (Qwen เท่านั้น), UX/DX split (Qwen เท่านั้น), vLLM path (Sonnet เท่านั้น)

---

## สรุปภาพรวม ณ ตอนนี้
**ลงมือได้เลย 5 ข้อ (A1–A5)** · **ต้องตัดสินใจ 1 ปม (Track A/B)** · **research เปิด 3 เส้น (C1–C3)** · **ปิดแล้ว 5 เส้น (D)** — เมื่อชุดที่ 4 มา จะ merge เป็นฉบับสุดท้ายแล้วค่อยจัดลำดับการลงมือจริง
