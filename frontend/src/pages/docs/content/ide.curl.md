```bash
# Cursor / Continue / Claude Code — set the OpenAI base URL:
#   base URL = http://127.0.0.1:8765/v1
#   API key  = anything (the server is local, no auth)
#   model    = any currently-loaded model id (see GET /v1/models)

curl http://127.0.0.1:8765/v1/models   # list ids you can put in the IDE
```
